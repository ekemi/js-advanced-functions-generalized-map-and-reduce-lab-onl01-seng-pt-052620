// Add your functions here
const map = function(arr ){
  return fn(arr);
  return fon(arr);
}

function fn (arr){
  let empty = []
  arr.forEach(ar =>{
    return (empty.push(ar*-1));
  })
  return empty;
}
 function fon (arr){
  let empty = []
  arr.forEach(ar =>{
    return (empty.push(ar));
  })
  return empty;
}
